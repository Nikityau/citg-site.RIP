export enum Slider_Type {
  DEFAULT = "default",
  VERTICAL = "vertical",
  HORIZONTAL = "horizontal",
  SQUARE = "square",
  SINGLE = "single",
  EXTENDED = "extended",
}

export enum Infinite_Type {
  INFINITE = "infinite",
  BORDER = "border",
  NO = "no",
}

export enum SliderElement_Type {
  DEFAULT = "default",
  CARD = "card",
}

export enum SliderBack_Type {
  DEFAULT = "default",
  HALF_BLACK = "half_black",
  FULL_BLACK = 'full_black',
  PARTY = 'party'
}

export type focus = "center" | "left" | "right" | "no";
